В качестве предметной области возьмем хз учителей, создают курсы

Сайт: https://ui.shadcn.com/

основные технологии шоб гуглилось:
prisma
next.js
betterauth
shadcnui

# Шаги
## Первый - инициализируем
```
npx shadcn@latest init -t next
```
Тыкаем
n
enter
enter
enter

## Второй - добавляем бд
сначало просто создаем бд в pgAdmin
После того как установилось переходим в созданную папку и пишем
```
npm install prisma --save-dev
npm install @prisma/client @prisma/adapter-pg pg dotenv 
npx prisma init
```
Затем открываем другую консоль, пишем npx prisma dev, копируем и вставляем в .env

## Третий - делаем странички и UI
Копируем и вставляем в components/theme-provider.tsx:
``` TypeScript
"use client"

import * as React from "react"
import { ThemeProvider as NextThemesProvider } from "next-themes"

export function ThemeProvider({
  children,
  ...props
}: React.ComponentProps<typeof NextThemesProvider>) {
  return <NextThemesProvider {...props}>{children}</NextThemesProvider>
}
```

Устанавливаем страницу логина/регистрации на выбор, например:

```
npx shadcn@latest add login-01
```
```
npx shadcn@latest add signup-01
```

Затем, устанавливаем href везде правильный (И заменяем а на Link) и удаляем ненужное
```
import Link from "next/link"
```

Везде на Input ставим name

В page.tsx можно пока что убрать весь код и вместо return сделать redirect("/dashboard")
import { redirect } from "next/navigation"

создаем папку dashboard и там layout.tsx

создаем в папке components navigation.tsx и используем ее в layout dashboard

## Четвертый Делаем авторизацию
```
npx prisma generate
```
Создаем файл prisma.ts в /lib
Вставляем:
``` TypeScript
import { PrismaClient } from "@/generated/prisma/client";
import { PrismaPg } from "@prisma/adapter-pg";

const adapter = new PrismaPg({
  connectionString: process.env.DATABASE_URL!,
});

const globalForPrisma = global as unknown as {
  prisma: PrismaClient;
};

const prisma =
  globalForPrisma.prisma ||
  new PrismaClient({
    adapter,
  });

if (process.env.NODE_ENV !== "production") globalForPrisma.prisma = prisma;

export default prisma;
```


```
npm install better-auth
```

После установки создаем секретик
```
npx auth@latest secret
```
(или сами 32 рандомные буковки)
А затем вставляем в .env
```
BETTER_AUTH_SECRET=aBmTTUqeSKdZE5qUUsKehbKjiPiFfG7g
```
Так же вставляем:
```
BETTER_AUTH_URL=http://localhost:3000 # Base URL of your app
```

Создаем файл auth.ts в /lib
``` TypeScript
import { betterAuth } from "better-auth";
import { prismaAdapter } from "better-auth/adapters/prisma";
import prisma from "@/lib/prisma";
import { nextCookies } from "better-auth/next-js";

export const auth = betterAuth({
  database: prismaAdapter(prisma, {
    provider: "postgresql",
  }),

  emailAndPassword: {
    enabled: true,
    requireEmailVerification: false,
  },
  plugins: [nextCookies()]
});
```

Если нам лень (а нам лень) создавать сущности пользователя:
```
npx auth generate
```
y

Затем, удаляем из схемы ненужное (подтверждение по почте например)

Делаем миграцию:
```
npx prisma migrate dev --name add-auth
```

Если вылетают ошибочки, пишем:
```
npx prisma db pull
npx prisma db push
```

И пишем:
```
npx prisma generate
```

(Альтернативно если там есть Pg Admin можете бд просто там создать и в Database url прописать, в тыщу раз проще будет)
```
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/gosi?schema=public"
```

## Делаем логин/регистрацию действия

Создаем папочку /server/auth-action.ts

``` TypeScript
"use server";
  
import { auth } from "@/lib/auth";
import { headers } from "next/headers";
  
export async function signUpEmail(formData: FormData) {
  const name = formData.get("name") as string;
  const email = formData.get("email") as string;
  const password = formData.get("password") as string;
  const confirmPassword = formData.get("confirm-password") as string;
  
  if (password !== confirmPassword) {
    return;
  }
  
  await auth.api.signUpEmail({
    body: { name, email, password },
    headers: await headers(),
  });
}

  
export async function signInEmail(formData: FormData) {
  const email = formData.get("email") as string;
  const password = formData.get("password") as string;
  const confirmPassword = formData.get("confirm-password") as string;

  
  if (password !== confirmPassword) {
    throw new Error("Passwords do not match");
  }

  
  await auth.api.signInEmail({
    body: { email, password },
    headers: await headers(),
  });
}

  
export async function signOut() {
  await auth.api.signOut({
    headers: await headers(),
  });
}
```

И закидываем их в форму, обязательно в login-form и signup-form в строку form action={signUpEmail} или InEmail

Создаем:
api/auth/[...all]/route.ts
``` TypeScript
import { auth } from "@/lib/auth"; // path to your auth file
import { toNextJsHandler } from "better-auth/next-js";

export const { POST, GET } = toNextJsHandler(auth);
```

``` TypeScript
import { createAuthClient } from "better-auth/react"

export const authClient = createAuthClient({
    /** The base URL of the server (optional if you're using the same domain) */
    baseURL: "http://localhost:3000"
})
```

Конец, дальше уже идем по гипотетическому примерчику

``` TypeScript
const { 
        data: session, 
        isPending, //loading state
        error, //error object
        refetch //refetch the session
    } = authClient.useSession()
```

``` TypeScript
import { NextRequest, NextResponse } from "next/server";

import { headers } from "next/headers";

import { auth } from "@/lib/auth";

  

export async function proxy(request: NextRequest) {

    const session = await auth.api.getSession({

        headers: await headers()

    })

  

    if(!session) {

        return NextResponse.redirect(new URL("/login", request.url));

    }

  

    return NextResponse.next();

}

  

export const config = {

  matcher: ["/dashboard"], // Specify the routes the middleware applies to

};
```
Если будет совсем все плохо и нужно будет делать REST API
```
export async function POST(request: Request) {
try {
const formData = await request.formData();

const title = formData.get("title") as string;
const content = formData.get("content") as string;
const authorId = formData.get("author-id") as string;

if (!title ?. trim() || !content ?. trim() || !authorId ?. trim()) {
return NextResponse. json(
{ error: "Не заполнены обязательные поля" },
{ status: 400 }

);
}

await prisma.post.create({
data: {
title: title.trim(),
content: content.trim(),
authorId: authorId.trim(),
},
});

revalidatePath("/dashboard");

return NextResponse.json({ success: true }, {status: 201});
} catch (error) {
console.error("create card error:", error);

return NextResponse.json(
{ error: "Ошибка при создании карточки" },
{ status: 500 }
);
}
}
```


```
async function handleSubmit(event: FormEvent<HTMLFormElement>) {
event.preventDefault();
setLoading(true);

try {
const formData = new FormData(event.currentTarget);

const response = await fetch("/api/cards", {
method: "POST",
body: formData,
});

const data = await response.json();

if (!response.ok) {
throw new Error(data ?. error || "Ошибка при создании карточки");
}

router.push("/dashboard");
router.refresh();
} catch (error) {
console.error(error);
} finally {
setLoading(false);
}
}
```
Создаёшь папку в /app/api/чеугодно
И в ней файл route.ts
И по всем законам физики, теперь это api обращение
На самом деле вызов серверных функций (которые сейчас у вас) под капотом это тот же самый api post запрос
Но если он будет доебывать (кто его знает) скажите
Вместо 

form action

В кнопке на onClick() бухаешь hadleSubmit вот такой, его в том же файле пишешь, где и сама формочка/карточка/что там у тебя

Если он прям супер мега гипер ультра не в настроении:
На случай если он встал с хуевой кровати и решил что: бля ты тварь мне нужен прям отдельный отдельный отдельный бек:


Создаёшь в корне проекта файл server.js, а затем просто код внутри серверных функций переносишь в него, кнопки оставляешь как в примере выше 

Вот условный пример:


const express = require('express');
const { PrismaClient } = require('@prisma/client');

const app = express();
const prisma = new PrismaClient();

app.use(express.json());

// GET /users
app.get('/users', async (req, res) => {
  const users = await prisma.user.findMany();
  res.json(users);
});

// GET /users/:id
app.get('/users/:id', async (req, res) => {
  const id = Number(req.params.id);
  const user = await prisma.user.findUnique({
    where: { id },
  });

  if (!user) {
    return res.status(404).json({ message: 'User not found' });
  }

  res.json(user);
});

// POST /users
app.post('/users', async (req, res) => {
  const { email, name } = req.body;

  const user = await prisma.user.create({
    data: { email, name },
  });

  res.status(201).json(user);
});

// PUT /users/:id
app.put('/users/:id', async (req, res) => {
  const id = Number(req.params.id);
  const { email, name } = req.body;

  const user = await prisma.user.update({
    where: { id },
    data: { email, name },
  });

  res.json(user);
});

// DELETE /users/:id
app.delete('/users/:id', async (req, res) => {
  const id = Number(req.params.id);

  await prisma.user.delete({
    where: { id },
  });

  res.status(204).send();
});

app.listen(3001, () => {
  console.log('API started on http://localhost:3001');
});